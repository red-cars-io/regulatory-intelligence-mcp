{
  "title": "Regulatory Intelligence MCP",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool Name",
      "type": "string",
      "description": "MCP tool to call",
      "enum": [
        "search_regulations",
        "entity_compliance_check",
        "regulatory_filing_tracker",
        "export_control_search",
        "compliance_risk_report"
      ],
      "editor": "select",
      "prefill": "search_regulations"
    },
    "params": {
      "title": "Parameters (JSON)",
      "type": "object",
      "description": "Tool parameters as JSON object",
      "editor": "json",
      "prefill": {}
    }
  }
}